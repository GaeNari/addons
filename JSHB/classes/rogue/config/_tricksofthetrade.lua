--
--	JSHB Rogue Options - tricks of the trade panel
--

if (select(2, UnitClass("player")) ~= "ROGUE") then return end

local L = _G.JSHB.L

function JSHB.Options:Panel_TricksOfTheTrade(ord)
	local DB = _G.JSHB.Options.DB
	return {
		order = ord,
		type = "group",
		name = GetSpellInfo(57934),
		args = {
			spacer1 = { order = 1, type = "description", name = "", desc = "", width = "full" },
			enable = {
				type = "toggle",
				order = 2,
				name = L["Enable"],
				width = "full",
				get = function(info) return DB.tricksofthetradeannounce.enable end,
				set = function(info, value) JSHB.Options:CollapseAll(); DB.tricksofthetradeannounce.enable = value;JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeModule) end,
			},
			enablemdmountwarn = {
				order = 12,
				type = "toggle",
				name = L["Whisper target if mounted"],
				width = "double",
				disabled = function(info) return not DB.tricksofthetradeannounce.enable end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeModule) end,
			},
			spacer14 = { order = 14, type = "description", name = "", desc = "", width = "full" },
			solochan = {
				order = 16,
				type = "select",
				name = L["Solo"],
				desc = L["SOLOCHANNEL_DESC"],
				disabled = function(info) return not DB.tricksofthetradeannounce.enable end,
				style = "dropdown",
				values = function() return(JSHB.chatChannels) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeModule) end,
			},
			partychan = {
				order = 18,
				type = "select",
				name = L["In a party"],
				desc = L["PARTYCHANNEL_DESC"],
				disabled = function(info) return not DB.tricksofthetradeannounce.enable end,
				style = "dropdown",
				values = function() return(JSHB.chatChannels) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeModule) end,
			},
			raidchan = {
				order = 20,
				type = "select",
				name = L["In a raid"],
				desc = L["RAIDCHANNEL_DESC"],
				disabled = function(info) return not DB.tricksofthetradeannounce.enable end,
				style = "dropdown",
				values = function() return(JSHB.chatChannels) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeModule) end,
			},
			arenachan = {
				order = 22,
				type = "select",
				name = L["In an arena"],
				desc = L["ARENACHANNEL_DESC"],
				disabled = function(info) return not DB.tricksofthetradeannounce.enable end,
				style = "dropdown",
				values = function() return(JSHB.chatChannels) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeModule) end,
			},
			pvpchan = {
				order = 24,
				type = "select",
				name = L["In a PvP zone"],
				desc = L["PVPCHANNEL_DESC"],
				disabled = function(info) return not DB.tricksofthetradeannounce.enable end,
				style = "dropdown",
				values = function() return(JSHB.chatChannels) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeModule) end,
			},
			spacer15 = { order = 25, type = "description", name = "", desc = "", width = "full" },
			clickenable = {
				type = "toggle",
				order = 26,
				name = L["Enable Right-Click to cast"],
				width = "full",
				get = function(info) return DB.tricksofthetradeannounce.clickenable end,
				set = function(info, value) JSHB.Options:CollapseAll(); DB.tricksofthetradeannounce.clickenable = value;JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeClickToCast) end,
			},
			fTARGET = {
				type = "toggle",
				order = 28,
				name = L["Target Frame"],
				width = "double",
				disabled = function(info) return (not DB.tricksofthetradeannounce.clickenable) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeClickToCast) end,
			},
			fTOT = {
				type = "toggle",
				order = 30,
				name = L["Target of Target Frame"],
				disabled = function(info) return (not DB.tricksofthetradeannounce.clickenable) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeClickToCast) end,
			},
			fFOCUS = {
				type = "toggle",
				order = 32,
				name = L["Focus Target Frame"],
				width = "double",
				disabled = function(info) return (not DB.tricksofthetradeannounce.clickenable) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeClickToCast) end,
			},
			fPARTY = {
				type = "toggle",
				order = 36,
				name = L["Party Frames"],
				width = "double",
				disabled = function(info) return (not DB.tricksofthetradeannounce.clickenable) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeClickToCast) end,
			},
			fPARTYPETS = {
				type = "toggle",
				order = 38,
				name = L["Party Pet Frames"],
				disabled = function(info) return (not DB.tricksofthetradeannounce.clickenable) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeClickToCast) end,
			},
			fRAID = {
				type = "toggle",
				order = 40,
				name = L["Raid Frames"],
				width = "double",
				disabled = function(info) return (not DB.tricksofthetradeannounce.clickenable) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeClickToCast) end,
			},
			fRAIDPET = {
				type = "toggle",
				order = 42,
				name = L["Raid Pet Frames"],
				disabled = function(info) return (not DB.tricksofthetradeannounce.clickenable) end,
				get = function(info) return(DB.tricksofthetradeannounce[info[#info] ]) end,
				set = function(info, value) DB.tricksofthetradeannounce[info[#info] ] = (value);JSHB.Options:LockDown(JSHB.SetupTricksOfTheTradeClickToCast) end,
			},
			note = {
				type = "description",
				order = 50,
				name = L["MDCLICKNOTE"],
				desc = "",
				width = "full",
				hidden = function(info) return (not DB.tricksofthetradeannounce.clickenable) end,
			},
		},
	}
end